/**
 * Profile settings tab components
 * Modular exports for different settings sections
 */

export { AccountSettings } from './AccountSettings';

// Note: The following components are currently still in ProfileSettings.tsx
// and should be extracted following the same pattern as AccountSettings:
// - CompanySettings
// - PreferencesSettings
// - DangerZoneSettings
